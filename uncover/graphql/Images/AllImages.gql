#import "./ImageFragment.gql"
query AllImages (offset: Int, $limit: Int){
  images_aggregate(offset: $offset, limit: $limit, order_by: {created_at: desc}) {
    aggregate {
      count
    }
    nodes {
      ...Image
    }
  }
}
